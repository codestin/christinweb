[build]
  command = "jekyll build --trace && npx pagefind --source _site"
  publish = "_site"

# Redirect old Writing URLs to Notes
[[redirects]]
  from = "/writing"
  to = "/notes"
  status = 301

[[redirects]]
  from = "/writing/*"
  to = "/notes"
  status = 301
